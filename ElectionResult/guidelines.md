Create TUI kind of interface to display the election results in a visually appealing way. Use libraries like `curses` or `rich` to create a dynamic and interactive terminal interface. Consider adding features like color coding for different parties, real-time updates, and the ability to filter results based on specific criteria (e.g., region, candidate).

The interface should show province, district, and constituency-wise results, along with the total votes for each candidate and party (max 5). You can also include a summary section that highlights the leading candidates and parties, as well as any significant changes in the results. The homepage shows province wise results showing wins and leads, make sure to show integrated results for that in the TUI as well.

The url to show these results is `https://election.ekantipur.com/?lng=eng`. You can use web scraping techniques to fetch the data from this URL and update the TUI interface in real-time as new results come in. Make sure to handle any potential issues with data fetching, such as network errors or changes in the website's structure. I think using `scrapling` library would be a good choice for web scraping in this case, as it provides a simple and efficient way to extract data from websites. Since it handles dynamic changing websites, it can be particularly useful for fetching election results that may update frequently. [Scrapling: https://github.com/D4Vinci/Scrapling]

The province, district and constituency-wise results can be shown using format: `https://election.ekantipur.com/pradesh-<pradesh_no>/district-<district_name>/constituency-<constituency_no>?lng=eng`. All those placeholders can be fetched from the page itself, and TUI can show options in order to switch to next province, district and constituency. Also, do note that, unless province selected, district and constituency option won't be shown in the election page. So, make sure you handle this and show proper option in TUI as well.

The TUI interface must show proper formatted tables with clear headings and color coding to differentiate between parties. Fetch latest data every 5 minutes to ensure that the displayed results are up-to-date. You can use a timer or scheduler to automate the data fetching process, and update the TUI interface accordingly. Additionally, consider implementing error handling to manage any issues that may arise during data fetching, such as network errors or changes in the website's structure. This will help ensure that the application remains robust and continues to function smoothly even in the face of unexpected challenges. Retry as well in network failures, maybe 4-5 times with exponential backoff strategy to avoid overwhelming the server with requests.

Just make everything sync to reduce complexity. Ensure TUI follows listening for keypress like htop, so that direct keypress can perform actions, rather than having separate section and asking for user input constantly. This will make the interface more intuitive and user-friendly, allowing users to navigate through the results and perform actions quickly and efficiently. For example, you can use arrow keys to navigate through different sections of the results, and specific keys (e.g., 'r' for manual refresh, 'f' for filter) to perform actions without needing to enter separate commands. This will enhance the overall user experience and make it easier for users to interact with the TUI interface. TUI must update in live mode, rather than console.print() way. This means that, instead of printing new results to the console every time there is an update, the TUI should dynamically update the existing interface with the new data. By implementing live updates, users will be able to see the latest election results without needing to refresh the entire interface, providing a more seamless and efficient experience.
